import assert from 'node:assert/strict';import test from 'node:test';import{readFileSync}from'node:fs';
test('Strategy Inspector renders selected persisted detail, recursive tree, and explicit actions',()=>{const source=readFileSync(new URL('../components/StrategyInspector.tsx',import.meta.url),'utf8');for(const text of ['SELECTED PERSISTED STRATEGY','Strategy Core','Rule logic','Health','Edit strategy','Duplicate','Activate','Archive','Restore','<Tree'])assert.match(source,new RegExp(text.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));const builder=readFileSync(new URL('../components/StrategyBuilder.tsx',import.meta.url),'utf8');assert.match(builder,/selectedProfile&&<StrategyInspector profile=\{profile\} rules=\{rules\} sessions=\{sessions\}/);assert.match(builder,/persistedStrategyToV2State\(profile,rules,sessions\)/);});
