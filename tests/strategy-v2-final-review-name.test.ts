import assert from 'node:assert/strict'; import test from 'node:test'; import { readFileSync } from 'node:fs';
test('V2 apply reaches a legacy Final Review with an editable validated persistence name',()=>{const source=readFileSync(new URL('../components/StrategyBuilder.tsx',import.meta.url),'utf8');assert.match(source,/handleV2Apply[\s\S]*setProfile\(persisted\.profile\)[\s\S]*setBuilderStep\('review'\)/);assert.match(source,/step-review[\s\S]*Strategy name<input value=\{profile\.name\}/);assert.match(source,/disabled=\{saving\|\|Boolean\(finalReviewNameError\)\|\|!finalReview\.canSave\}/);});
