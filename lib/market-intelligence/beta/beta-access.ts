import type { MarketIntelligenceV2Flags } from '../feature-flags.ts';
export type BetaActor = { userId: string; role: string | null; betaTester: boolean };
export type BetaAccess = { eligible: boolean; admin: boolean; diagnosticsAllowed: boolean; reason: 'DISABLED' | 'ADMIN' | 'BETA_TESTER' | 'INELIGIBLE' };
const ADMIN_ROLES = new Set(['OWNER', 'ADMIN', 'SECURITY_ADMIN']);
export function resolveBetaAccess(flags: MarketIntelligenceV2Flags, actor: BetaActor): BetaAccess { const admin = actor.role !== null && ADMIN_ROLES.has(actor.role.toUpperCase()); if (!flags.enabled) return Object.freeze({ eligible: false, admin, diagnosticsAllowed: false, reason: 'DISABLED' }); if (flags.adminOnly && !admin) return Object.freeze({ eligible: false, admin, diagnosticsAllowed: false, reason: 'INELIGIBLE' }); if (flags.betaOnly && !admin && !actor.betaTester) return Object.freeze({ eligible: false, admin, diagnosticsAllowed: false, reason: 'INELIGIBLE' }); return Object.freeze({ eligible: true, admin, diagnosticsAllowed: admin, reason: admin ? 'ADMIN' : 'BETA_TESTER' }); }
export function betaActorFromServerIdentity(user: { id: string; app_metadata?: Record<string, unknown> }, role: string | null, allowlist: readonly string[]): BetaActor { return Object.freeze({ userId: user.id, role, betaTester: user.app_metadata?.market_intelligence_v2_beta === true || allowlist.includes(user.id) }); }
