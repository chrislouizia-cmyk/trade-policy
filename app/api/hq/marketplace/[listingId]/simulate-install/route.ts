import {NextResponse} from 'next/server';import {getHQMarketplaceContext} from '@/lib/server/hq-marketplace';
export async function POST(request:Request,{params}:{params:Promise<{listingId:string}>}){try{const {listingId}=await params,{supabase}=await getHQMarketplaceContext();const body=await request.json().catch(()=>null);if(typeof body?.recipientUserId!=='string')return NextResponse.json({error:'Recipient is required.'},{status:400});const {data,error}=await supabase.rpc('staff_marketplace_simulate_install',{p_listing_id:listingId,p_recipient_user_id:body.recipientUserId});if(error)return NextResponse.json({error:'Internal simulated install failed.'},{status:400});return NextResponse.json({install:data},{headers:{'Cache-Control':'no-store'}})}catch{return NextResponse.json({error:'Marketplace Lab access denied.'},{status:403})}}
