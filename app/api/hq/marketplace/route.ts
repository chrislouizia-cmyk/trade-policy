import {NextResponse} from 'next/server';import {createAdminClient} from '@/lib/supabase/admin';import {getHQMarketplaceContext,toMarketplacePreview} from '@/lib/server/hq-marketplace';
export const dynamic='force-dynamic';
export async function GET(){try{await getHQMarketplaceContext();const admin=createAdminClient();const {data,error}=await admin.from('marketplace_listings').select('id,release_id,review_status,sanitized_metadata,marketplace_strategy_releases(release_version),marketplace_release_rankings(score_version,performance_score,marketplace_readiness_score,calculated_at)').order('created_at',{ascending:false}).limit(100);if(error)throw error;return NextResponse.json({items:(data??[]).map(toMarketplacePreview)},{headers:{'Cache-Control':'private, no-store'}})}catch{return NextResponse.json({error:'Marketplace Lab unavailable.'},{status:403})}}
