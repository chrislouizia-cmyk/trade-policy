import {NextResponse} from 'next/server';import {createAdminClient} from '@/lib/supabase/admin';import {getHQMarketplaceContext,toMarketplacePreview} from '@/lib/server/hq-marketplace';
export const dynamic='force-dynamic';
const safeCode=(error:unknown)=>{const code=typeof (error as {code?:unknown})?.code==='string'?(error as {code:string}).code:'';return code==='42P01'||code==='PGRST205'?'MARKETPLACE_SCHEMA_UNAVAILABLE':'MARKETPLACE_CATALOG_UNAVAILABLE'};
const logSupabaseQueryFailure=(label:string,error:unknown)=>{const candidate=error as {code?:unknown;message?:unknown;details?:unknown;hint?:unknown};console.error('[HQ marketplace catalog query failure]',{label,code:typeof candidate?.code==='string'?candidate.code:null,message:typeof candidate?.message==='string'?candidate.message:null,details:typeof candidate?.details==='string'?candidate.details:null,hint:typeof candidate?.hint==='string'?candidate.hint:null});};
export async function GET(request:Request){try{await getHQMarketplaceContext()}catch{return NextResponse.json({error:'Marketplace Lab access denied.',code:'FORBIDDEN'},{status:403})}try{const admin=createAdminClient();const {data:listings,error:listingsError}=await admin.from('marketplace_listings').select('id,release_id,review_status,sanitized_metadata,created_at').order('created_at',{ascending:false}).limit(100);if(listingsError){logSupabaseQueryFailure('listings',listingsError);throw listingsError;}const releaseIds=[...new Set((listings??[]).map((row:any)=>row.release_id).filter((value):value is string=>Boolean(value)))];const releaseById=new Map<string,any>();if(releaseIds.length>0){const {data:releases,error:releasesError}=await admin.from('marketplace_strategy_releases').select('id,release_version').in('id',releaseIds);if(releasesError){logSupabaseQueryFailure('releases',releasesError);throw releasesError;}for(const row of (releases??[])){if(!row.id)continue;releaseById.set(row.id,row);}}const rankingByRelease=new Map<string,any>();if(releaseIds.length>0){const {data:rankings,error:rankingsError}=await admin.from('marketplace_release_rankings').select('release_id,score_version,performance_score,marketplace_readiness_score,rank_position,calculated_at').in('release_id',releaseIds).order('calculated_at',{ascending:false});if(rankingsError){logSupabaseQueryFailure('rankings',rankingsError);throw rankingsError;}for(const row of (rankings??[])){if(!row.release_id||rankingByRelease.has(row.release_id))continue;rankingByRelease.set(row.release_id,row);}}return NextResponse.json({items:(listings??[]).map((row:any)=>toMarketplacePreview({ ...row, release_version:releaseById.get(row.release_id)?.release_version ?? 0, marketplace_release_rankings: rankingByRelease.has(row.release_id)?[rankingByRelease.get(row.release_id)]:[] }))},{headers:{'Cache-Control':'private, no-store'}})}catch(error){const code=safeCode(error);return NextResponse.json({error:code==='MARKETPLACE_SCHEMA_UNAVAILABLE'?'Marketplace Foundation schema is not provisioned yet.':'Marketplace catalog could not be loaded.',code},{status:503})}}
