import { getHQContext, HQShell } from '@/lib/hq-page';
export default async function Page(){const {supabase,role,displayName,permissions}=await getHQContext('organizations.view');const {data:orgId}=await supabase.rpc('ensure_internal_organization');return <HQShell displayName={displayName} role={role} permissions={permissions}><div className="stack"><section className="card hq-hero-card"><div><span className="eyebrow">ORGANIZATIONS</span><h1>Trade Police HQ</h1><p>Company boundaries for staff, customers and future white-label firms.</p></div><span className="status-pill healthy">ACTIVE</span></section><div className="grid grid-3 metric-grid"><div className="card metric"><span className="muted">Organization type</span><strong>Internal</strong></div><div className="card metric"><span className="muted">Privacy model</span><strong>Least privilege</strong></div><div className="card metric"><span className="muted">Organization ID</span><small>{orgId||'Run migration 011'}</small></div></div><section className="card"><h2>What organizations control</h2><div className="grid grid-2 hq-info-grid"><div><strong>People and permissions</strong><p className="muted">Staff membership, role assignments and individual access.</p></div><div><strong>Customer boundaries</strong><p className="muted">Future firms can remain isolated without exposing customer strategy data.</p></div><div><strong>Brand and plans</strong><p className="muted">Prepared for partner and white-label configuration.</p></div><div><strong>Auditability</strong><p className="muted">Privileged actions remain attributable to an individual account.</p></div></div></section></div></HQShell>}
