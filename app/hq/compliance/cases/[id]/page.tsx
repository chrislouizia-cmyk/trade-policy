import {notFound} from 'next/navigation';import ComplianceCaseDetail from '@/components/hq/ComplianceCaseDetail';import {getHQContext,HQShell} from '@/lib/hq-page';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params,{supabase,role,displayName,permissions}=await getHQContext('compliance.view'),{data,error}=await supabase.rpc('staff_compliance_case',{p_case_id:id});if(error)throw new Error('Compliance case or evidence query failed.');if(!data)notFound();return <HQShell displayName={displayName} role={role} permissions={permissions}><ComplianceCaseDetail item={data} canManage={permissions.includes('compliance.manage')}/></HQShell>}
