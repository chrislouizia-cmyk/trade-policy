import SystemOperations from '@/components/hq/SystemOperations';import {getHQContext,HQShell} from '@/lib/hq-page';export default async function Page(){const {supabase,role,displayName,permissions}=await getHQContext('system.health');const [{data:overview,error},{data:incidents}]=await Promise.all([supabase.rpc('staff_system_operations'),supabase.rpc('staff_system_incidents',{p_limit:100})]);if(error)throw new Error(error.message);return <HQShell displayName={displayName} role={role} permissions={permissions}><SystemOperations overview={overview??{}} incidents={(incidents??[]).filter((x:any)=>!x.resolved_at)}/></HQShell>}
