import WorkspaceDashboard from '@/components/hq/WorkspaceDashboard';import {getHQContext,HQShell} from '@/lib/hq-page';export default async function Page(){const {supabase,role,displayName,permissions}=await getHQContext('system.health');const {data}=await supabase.rpc('staff_workspace_overview');return <HQShell displayName={displayName} role={role} permissions={permissions}><WorkspaceDashboard overview={data??{}} role="TECHNICIAN"/></HQShell>}
