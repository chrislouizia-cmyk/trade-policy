import {redirect} from 'next/navigation';import SalesDraftComposer from '@/components/hq/SalesDraftComposer';import {getHQContext,HQShell} from '@/lib/hq-page';
export default async function Page({searchParams}:{searchParams:Promise<{customer?:string}>}){const {customer}=await searchParams,{supabase,role,displayName,permissions}=await getHQContext('sales.manage');if(!customer)redirect('/hq/sales');const {data,error}=await supabase.rpc('staff_sales_email_context',{p_customer_id:customer});if(error||!data)redirect('/hq/sales');return <HQShell displayName={displayName} role={role} permissions={permissions}><SalesDraftComposer customer={data}/></HQShell>}
