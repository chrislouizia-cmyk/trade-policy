import SalesCRM from '@/components/hq/SalesCRM';import {getHQContext,HQShell} from '@/lib/hq-page';export default async function Page(){const {supabase,role,displayName,permissions}=await getHQContext('sales.view');const {data,error}=await supabase.rpc('staff_sales_customer_crm',{p_limit:2000});if(error)throw new Error(`Sales CRM failed: ${error.message}`);return <HQShell displayName={displayName} role={role} permissions={permissions}><SalesCRM rows={data??[]}/></HQShell>}
