import Link from 'next/link';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {getBillingState} from '@/lib/billing/entitlements';
export default async function BillingSuccess(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/client/login?next=/billing/success');const state=await getBillingState(user.id);return <main className="login-shell"><section className="card login-card"><p className="eyebrow">CHECKOUT COMPLETE</p><h1>{state.plan==='PRO'?'Pro is active':'Payment is processing'}</h1><p>{state.plan==='PRO'?'Your server-verified Stripe subscription is active.':'Your return from Checkout does not grant access by itself. We are waiting for the verified Stripe webhook.'}</p><Link className="button-link primary" href="/account">View account</Link></section></main>}
